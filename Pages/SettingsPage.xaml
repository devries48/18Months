<ContentPage
    x:Class="Months18.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:helpers="clr-namespace:Months18.Helpers">
    <Grid>
        <Grid RadioButtonGroup.GroupName="{Binding GroupName}" RadioButtonGroup.SelectedValue="{Binding Selection}">
            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
                <RowDefinition Height="30" />
                <RowDefinition Height="50" />
                <RowDefinition Height="10" />
                <RowDefinition Height="30" />
                <RowDefinition Height="50" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label
                Grid.ColumnSpan="2"
                class="SectionHeader"
                Text="Connection" />
            <RadioButton
                Grid.Row="1"
                Grid.ColumnSpan="2"
                Value="Local">
                <RadioButton.Content>
                    <Label class="Primary" Text="LOCAL, select path to data folder" />
                </RadioButton.Content>
            </RadioButton>
            <Entry
                x:Name="LocalDataPath"
                Grid.Row="2"
                Grid.Column="1"
                Placeholder="Enter path"
                HorizontalOptions="FillAndExpand" />

            <RadioButton
                Grid.Row="4"
                Grid.ColumnSpan="2"
                Value="Remote"
                HeightRequest="20">
                <RadioButton.Content>
                    <Label class="Primary" Text="REMOTE, select URL to MusicMate Server" />
                </RadioButton.Content>
            </RadioButton>
            <Entry
               x:Name="RemoteUrlPath"
               Grid.Row="5"
               Grid.Column="1"
               Placeholder="Enter URL"
               HorizontalOptions="FillAndExpand" />

        </Grid>
    </Grid>

</ContentPage>